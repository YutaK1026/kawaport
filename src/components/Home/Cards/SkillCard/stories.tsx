import SkillCard from ".";

const meta = {
  title: "SkillCard",
  component: SkillCard,
};

export default meta;

export const Default = () => {
  return <SkillCard />;
};
Default.storyName = "SkillCard";
