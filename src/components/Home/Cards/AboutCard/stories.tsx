import AboutCard from ".";

const meta = {
  title: "AboutCard",
  component: AboutCard,
};

export default meta;

export const Default = () => {
  return <AboutCard />;
};
Default.storyName = "AboutCard";
