import ProjectCard from ".";

const meta = {
  title: "ProjectCard",
  component: ProjectCard,
};

export default meta;

export const Default = () => {
  return <ProjectCard />;
};
Default.storyName = "ProjectCard";
